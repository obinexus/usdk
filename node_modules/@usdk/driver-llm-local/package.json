{
  "name": "@usdk/driver-llm-local",
  "version": "0.1.0",
  "private": true,
  "description": "A labeled, honest capability-llm stub for a real local-inference backend. No local LLM runtime/model was confirmed available in this environment (no ONNX Runtime Web / WebGPU / WASM model runtime, no local model weights) - see docs/IMPLEMENTATION_STATUS.md 'Vertical slice'. isAvailable() returns false; generate() always rejects rather than fabricating a response or silently degrading to the fixture (a fixture must never be labeled as an LLM - this task's explicit instruction).",
  "type": "module",
  "main": "src/index.mjs",
  "exports": { ".": "./src/index.mjs" },
  "dependencies": { "@usdk/contracts": "0.1.0", "@usdk/capability-llm": "0.1.0" },
  "engines": { "node": ">=20" }
}
