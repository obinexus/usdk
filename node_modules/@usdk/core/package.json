{
  "name": "@usdk/core",
  "version": "0.1.0",
  "private": true,
  "description": "Backend-independent candidate lifecycle and agreement state machine - the JS analogue of src/core/core.c. No capability drivers, no UI, no interpreter/browser-specific APIs beyond the journal storage adapter interface (journal.mjs), which is itself pluggable (MemoryJournal works everywhere; FileJournal is Node-only). Depends only on @usdk/contracts.",
  "type": "module",
  "main": "src/index.mjs",
  "exports": { ".": "./src/index.mjs" },
  "dependencies": { "@usdk/contracts": "0.1.0" },
  "engines": { "node": ">=20" }
}
