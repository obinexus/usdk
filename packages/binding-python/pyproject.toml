[project]
name = "usdk-binding-python"
version = "0.1.0"
description = "ctypes bindings for the native USDK C ABI (usdk-perceive role only in this release - see README.md). No third-party dependencies - stdlib ctypes only."
requires-python = ">=3.9"

[tool.setuptools]
packages = ["usdk"]
